params {
    data = "./data/"
    results = "./results/"

    query_dir = "query"
    query_mode = "consensus"
    
    reference_dir = "reference"
    reference_mode = null

    n = 30
}

docker {
    enabled = true
}